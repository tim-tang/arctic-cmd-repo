define(function(require) {
    var module = require('./dist/bootstrap-jasny');
    describe('bootstrap-jasny', function() {
        it('should has VERSION', function() {
            //expect(module.VERSION).to.be.a('string');
        });
    });
});
