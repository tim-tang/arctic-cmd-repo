define(function(require) {
    var module = require('./dist/bootstrap-touchspin');
    var expect = require('expect');
    describe('bootstrap-touchspin', function() {
        it('should has VERSION', function() {
            //expect(module.VERSION).to.be.a('string');
        });
    });
});
