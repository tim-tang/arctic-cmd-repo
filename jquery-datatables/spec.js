define(function(require) {
    var $ = require('$');
    var module = require('./dist/datatables-debug');
    var expect = require('expect');
    describe('datatables', function() {
        it('should has dataTable method', function() {
            //expect($.fn.dataTable).to.be.a('function');
        });
    });
});
